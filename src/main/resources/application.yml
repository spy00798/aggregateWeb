spring:
  devtools:
    livereload:
      enabled : true
  thymeleaf:
    prefix : classpath:/templates/
    suffix : .html
    cache : false
    check-template-location : true
  datasource:
    main:
      driverClassName : org.mariadb.jdbc.Driver
      jdbc-url : jdbc:mysql://localhost:3306/aggregate?characterEncoding=UTF-8&amp;allowMultiQueries=true&autoReconnect=true
      username : incruit
      password : test

